// Two isolated browser profiles. Only disposable local QA services; never production.
import {spawn} from 'node:child_process'
import fs from 'node:fs/promises'
import os from 'node:os'
import path from 'node:path'
const FRONT='http://127.0.0.1:5273',ADMIN='http://127.0.0.1:4273',API='http://127.0.0.1:8879/api/v1',password='Isolated-QA-2026!'
const wait=ms=>new Promise(r=>setTimeout(r,ms)),browsers=[]
async function browser(port){const folder=await fs.mkdtemp(path.join(os.tmpdir(),'hufe-session-qa-')),process=spawn('/Applications/Google Chrome.app/Contents/MacOS/Google Chrome',['--headless=new','--no-first-run','--no-default-browser-check','--disable-background-networking',`--user-data-dir=${folder}`,`--remote-debugging-port=${port}`,'about:blank'],{stdio:'ignore'});let tabs;for(let i=0;i<60;i++){try{tabs=await(await fetch(`http://127.0.0.1:${port}/json/list`)).json();if(tabs.length)break}catch{}await wait(100)}const socket=new WebSocket(tabs.find(t=>t.type==='page').webSocketDebuggerUrl);await new Promise((resolve,reject)=>{socket.onopen=resolve;socket.onerror=reject});let id=0;const pending=new Map(),events=[];socket.onmessage=e=>{const message=JSON.parse(e.data);if(message.id){const p=pending.get(message.id);pending.delete(message.id);message.error?p.reject(Error(JSON.stringify(message.error))):p.resolve(message.result)}else events.push(message)};const send=(method,params={})=>new Promise((resolve,reject)=>{const key=++id;pending.set(key,{resolve,reject});socket.send(JSON.stringify({id:key,method,params}))});const evaluate=async expression=>{const result=await send('Runtime.evaluate',{expression,returnByValue:true,awaitPromise:true,userGesture:true});if(result.exceptionDetails)throw Error(result.exceptionDetails.exception?.description||result.exceptionDetails.text);return result.result.value};const until=async(expression,label,limit=12000)=>{const start=Date.now();while(Date.now()-start<limit){if(await evaluate(expression))return;await wait(120)}throw Error('Timed out: '+label)};await send('Page.enable');await send('Runtime.enable');await send('Network.enable');await send('Emulation.setDeviceMetricsOverride',{width:1280,height:900,deviceScaleFactor:1,mobile:false});const value={folder,process,socket,send,evaluate,until,events};browsers.push(value);return value}
async function fill(b,selector,value){await b.evaluate(`(()=>{const el=document.querySelector(${JSON.stringify(selector)});el.value=${JSON.stringify(value)};el.dispatchEvent(new Event('input',{bubbles:true}))})()`)}
async function frontendLogin(b){await b.send('Page.navigate',{url:FRONT+'/#/pages/verify/index'});await b.until(`!!document.querySelector('.field-box input[type="text"]')`,'front login form');await fill(b,'.field-box input[type="text"]','qa_member');await fill(b,'.field-box input[type="password"]',password);await wait(100);await b.evaluate(`document.querySelector('.login-button').click()`);await b.until(`!!uni.getStorageSync('hufe_alumni_auth_state')?.accessToken&&location.hash.includes('mine')`,'front login and mine');return b.evaluate(`uni.getStorageSync('hufe_alumni_auth_state').accessToken`)}
const me=async token=>{const r=await fetch(API+'/me',{headers:{authorization:'Bearer '+token}});return {status:r.status,body:await r.json()}}
try{
  const a=await browser(9510),b=await browser(9511),first=await frontendLogin(a)
  const idA=await a.evaluate(`localStorage.getItem('hufe.installation.device-id.v1')`)
  await a.send('Page.reload');await a.until(`!!window.uni&&!!document.querySelector('.mine-page')`,'reload mine')
  if(await a.evaluate(`uni.getStorageSync('hufe_alumni_auth_state').accessToken`)!==first)throw Error('Front reload lost token')
  const wrong=await fetch(API+'/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({username:'qa_member',password:'IntentionallyIncorrect2026!',deviceId:'wrong_password_device'})});if(wrong.status!==401||(await me(first)).status!==200)throw Error('Wrong password displaced active device')
  const sameLogin=await(await fetch(API+'/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({username:'qa_member',password,deviceId:idA})})).json();if(!sameLogin.data?.accessToken||sameLogin.data.expiresAt!==null||sameLogin.data.sessionPolicy?.expiresAutomatically!==false||(await me(first)).status!==200)throw Error('Same device login/persistent policy mismatch')
  await a.evaluate(`window.__sessionEndReason='';uni.$on('hufe-auth-changed',event=>{if(event.status==='signed_out')window.__sessionEndReason=event.reason||''})`)
  const second=await frontendLogin(b),idB=await b.evaluate(`localStorage.getItem('hufe.installation.device-id.v1')`)
  if(idA===idB)throw Error('Isolated installations shared a device label')
  const denied=await me(first);if(denied.status!==401||denied.body.code!=='SESSION_REPLACED')throw Error('Old device not rejected')
  await a.until(`!!window.uni&&!uni.getStorageSync('hufe_alumni_auth_state')?.accessToken`,'first device automatic logout',40000)
  const reason=await a.evaluate(`window.__sessionEndReason`);if(!reason.includes('其他设备'))throw Error('Missing clear replacement reason')
  await b.send('Page.reload');await b.until(`!!window.uni&&!!document.querySelector('.mine-page')`,'second device reload');if(await b.evaluate(`uni.getStorageSync('hufe_alumni_auth_state').accessToken`)!==second||(await me(second)).status!==200)throw Error('Second device persistence failed')
  const screenshot=await a.send('Page.captureScreenshot',{format:'png'});await fs.writeFile(path.resolve('.local-runtime/session-old-device-qa.png'),Buffer.from(screenshot.data,'base64'))
  await b.send('Page.navigate',{url:ADMIN+'/#/login'});await b.until(`!!document.querySelector('input[autocomplete="username"]')`,'admin login form');await fill(b,'input[autocomplete="username"]','qa_admin');await fill(b,'input[autocomplete="current-password"]',password);await wait(100);await b.evaluate(`document.querySelector('button[type="submit"]').click()`);await b.until(`!!localStorage.getItem('hufe.admin.access-token')&&!location.hash.includes('login')`,'admin login');const adminToken=await b.evaluate(`localStorage.getItem('hufe.admin.access-token')`);await b.send('Page.reload');await b.until(`!!document.querySelector('.admin-shell')`,'admin reload authenticated');if(await b.evaluate(`localStorage.getItem('hufe.admin.access-token')`)!==adminToken||await b.evaluate(`sessionStorage.getItem('hufe.admin.access-token')`))throw Error('Admin persistence migration mismatch')
  const adminShot=await b.send('Page.captureScreenshot',{format:'png'});await fs.writeFile(path.resolve('.local-runtime/session-admin-reload-qa.png'),Buffer.from(adminShot.data,'base64'))
  let serviceRestartPersistent=false
  if(process.env.HUFE_SESSION_QA_RESTART==='1'){
    console.log('QA_RESTART_READY: restart only the disposable 8879 service with its same temporary database, then send continue.')
await new Promise((resolve,reject)=>{const timeout=setTimeout(()=>reject(Error('QA restart checkpoint timed out')),180000);process.stdin.setEncoding('utf8');process.stdin.once('data',value=>{process.stdin.pause();clearTimeout(timeout);String(value).trim()==='continue'?resolve():reject(Error('Unexpected QA checkpoint input'))})})
    if((await me(second)).status!==200||(await me(adminToken)).status!==200||(await me(first)).body.code!=='SESSION_REPLACED')throw Error('Session policy lost across service restart')
    await b.send('Page.reload');await b.until(`!!document.querySelector('.admin-shell')`,'admin persisted after API restart')
    serviceRestartPersistent=true
  }
  const exceptions=[...a.events,...b.events].filter(e=>e.method==='Runtime.exceptionThrown').map(e=>e.params.exceptionDetails.exception?.description||e.params.exceptionDetails.text)
  console.log(JSON.stringify({frontReloadPersistent:true,wrongPasswordDoesNotKick:true,sameDeviceReloginKeepsPriorToken:true,distinctRandomDevices:true,oldDeviceServerCode:denied.body.code,oldDeviceAutomaticLogout:true,friendlyReason:reason,newDeviceReloadPersistent:true,adminReloadPersistent:true,serviceRestartPersistent,expiresAutomatically:false,exceptions},null,2));if(exceptions.length)process.exitCode=1
}catch(error){for(let i=0;i<browsers.length;i++){const b=browsers[i];console.log({exceptions:b.events.filter(e=>e.method==='Runtime.exceptionThrown').map(e=>e.params.exceptionDetails.exception?.description||e.params.exceptionDetails.text)});console.log(await b.evaluate(`({url:location.href,text:document.body.innerText.slice(0,1800),inputs:[...document.querySelectorAll('input')].map(i=>({type:i.type,placeholder:i.placeholder,autocomplete:i.autocomplete}))})`).catch(()=>({})));const shot=await b.send('Page.captureScreenshot',{format:'png'}).catch(()=>null);if(shot)await fs.writeFile(path.resolve(`.local-runtime/session-browser-error-${i}.png`),Buffer.from(shot.data,'base64'))}throw error}finally{for(const b of browsers){b.socket.close();b.process.kill('SIGTERM')}await wait(700);for(const b of browsers)await fs.rm(b.folder,{recursive:true,force:true})}
