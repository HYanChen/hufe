# 参与维护

提交前请阅读 [开发指南](docs/DEVELOPMENT.md)与 [NOTICE](NOTICE.md)。公开仓库尚未为自有代码授予通用开源许可证；贡献内容必须具有合法来源，不提交未经授权的学校材料、地图或第三方数据。

## 问题反馈

Issues 中写明版本、终端、复现步骤、预期和实际结果，只附脱敏截图与日志。密码、学号、聊天记录、token、SQL、证件材料不进入公开讨论。安全问题遵循 [SECURITY](SECURITY.md)。

## Pull Request

1. 从当前默认分支建立工作分支，一个 PR 聚焦一个问题。
2. 按根锁文件安装，不提交 node_modules、成品、运行日志、数据库、用户文件或真实 .env。
3. 同步后端授权、前后台入口、模块开关、审计和测试；隐藏按钮不等于权限控制。
4. 运行 `pnpm validate`、`pnpm client:test`、`pnpm admin:test`、`pnpm server:test` 并构建受影响的端。
5. UI 变更提供桌面/手机交互证据；多端变更区分编译、开发者工具和真机验收。
6. 说明变更、已测/未测项、迁移与回退要求。测试未执行不能标为通过。

真实 MySQL 测试只使用专门隔离库，禁止指向生产库或本地业务库。测试中的姓名、学号等必须完全虚构，不以真实材料作为夹具。

## 文档和发布

修改脚本与配置后同步相应指南。源码 commit 不自动更新 Release 二进制；发布包独立构建并校验 SHA。公开文档不记录具体生产数据库名、私有地址、账号数量或备份现场。保留第三方许可证，不擅自给学校品牌授予开源授权。
